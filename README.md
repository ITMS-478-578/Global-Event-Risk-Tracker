# TravelTracker
Provides safety & comfor score based of the weather, any news, currency exchange rate, and number of hospitals and police stations nearby.

For libraries, in terminal run: py -m pip install -r requirements.txt

If this gives you an issue, create a virtual environment.
Do this by pressing Ctrl + P, typing Create virtual environment.
Create a new terminal and run command.